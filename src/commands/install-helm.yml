description: >
  installing hadolint
parameters:
  version:
    type: string
    default: "v3.5.3"
    description: "hadolint version"
steps:
  - helm/install-helm-client:
      version: << parameters.version >>
