description: >
  installing helm client
parameters:
  version:
    type: string
    default: "v3.5.3"
    description: "helm version"
steps:
  - helm/install-helm-client:
      version: << parameters.version >>
